# A simple NodeJS  app using Github API and Express

The nodejs app shows the top 10 repositories in Github. It uses Github API to query the repositories. 

Details:

- Run npm install to install the necessary node modules
- Default port is 3000
- Default language it queries for is Javascript. Can be changed in the code

Feel free to use and modify the code for learning or any other uses.